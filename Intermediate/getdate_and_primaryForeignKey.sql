--getdate() and primary/foriegn key
select GETDATE() --gives current date and time

--create table Students(
--student_id int primary key,
--name varchar(50)
--);
--create table Marks(
--mark_id int primary key,
--student_id int,
--marks int,
--foreign key (student_id) references Students(student_id)
--);